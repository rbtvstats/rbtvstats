describe('controller', function() {

  beforeEach(module('app.data.live'));

  it('should ...', inject(function(controller) {

    //expect(controller.doSomething()).toEqual('something');

  }));

});
