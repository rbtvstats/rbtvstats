describe('duration', function() {

    beforeEach(module('app.common'));

    it('should ...', inject(function($filter) {

        var filter = $filter('duration');

        expect(filter('input')).toEqual('output');

    }));

});
