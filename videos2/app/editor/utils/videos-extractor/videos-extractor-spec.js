describe('videosExtractor', function() {

  beforeEach(module('app.editor'));

  it('should ...', inject(function(videosExtractor) {

    //expect(videosExtractor.doSomething()).toEqual('something');

  }));

});
