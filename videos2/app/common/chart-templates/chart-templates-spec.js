describe('chartTemplates', function() {

  beforeEach(module('app.common'));

  it('should ...', inject(function(chartTemplates) {

    //expect(chartTemplates.doSomething()).toEqual('something');

  }));

});
